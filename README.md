# 🎓 IFNTUOG Schedule Bot (НУНГ Розклад)

Сучасний Telegram-бот для перегляду розкладу занять Івано-Франківського національного технічного університету нафти і газу (НУНГ).

Бот автоматично отримує дані з API деканату, генерує зручні зображення розкладу (замість тексту) та надсилає автоматичні сповіщення.

## 🔥 Можливості

* 🖼 **Графічний розклад:** Генерація стильних картинок з розкладом на день або тиждень.
* 📅 **Гнучка навігація:** Перегляд розкладу на сьогодні, завтра, поточний тиждень або будь-яку дату.
* 🔔 **Автоматичні сповіщення:**

  * Щоденна розсилка розкладу на завтра (о 17:00).
  * Тижнева розсилка у неділю (о 17:00).
* 🔄 **Моніторинг змін:** Відстежує зміни в розкладі та повідомляє про них (beta).
* 🔎 **Пошук:**

  * `/search [текст]` — пошук предмета у розкладі вашої групи.
  * `/search_all [текст]` — глобальний пошук викладачів або аудиторій.
* 🛡 **Адміністрування:** Налаштування групи та керування сповіщеннями доступні лише адміністраторам чату.

## 🛠 Встановлення

### 1. Клонування репозиторію

```bash
git clone https://github.com/Dr34mChas3r/IFNTUOGrozkladTELEGRAMbot.git
cd IFNTUOGrozkladTELEGRAMbot
```

### 2. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 3. Налаштування середовища

Створіть файл `.env` у кореневій папці проєкту та додайте ваш токен:

```env
BOT_TOKEN=ваш_токен_від_BotFather
```

**Важливо:** Переконайтеся, що у папці з ботом є файл шрифту (наприклад, `arial.ttf` або `Roboto-Regular.ttf`), який вказаний у налаштуваннях `bot_global.py`. Без нього текст на картинках може не відображатися.

### 4. Тестовий запуск

```bash
python3 bot_global.py
```

## 🤖 Команди бота

| Команда               | Опис                                        |
| --------------------- | ------------------------------------------- |
| `/start`              | Головне меню та привітання                  |
| `/group [Назва]`      | Встановити групу для чату (напр. `КІ-24-1`) |
| `/today`              | Отримати картинку розкладу на сьогодні      |
| `/tomorrow`           | Отримати картинку розкладу на завтра        |
| `/week`               | Отримати картинку розкладу на тиждень       |
| `/date [ДД.ММ]`       | Розклад на конкретну дату                   |
| `/search [текст]`     | Локальний пошук (у межах групи)             |
| `/search_all [текст]` | Глобальний пошук (викладач / аудиторія)     |
| `/settings`           | Налаштування сповіщень (тільки для адмінів) |

---

## ⚙️ Налаштування автозапуску (systemd)

Щоб бот працював у фоновому режимі та автоматично перезапускався після перезавантаження сервера, створіть службу `systemd`.

### 1. Створення файлу служби

```bash
sudo nano /etc/systemd/system/nung_bot.service
```

Вставте наступний вміст (відредагуйте шляхи відповідно до вашого середовища):

```ini
[Unit]
Description=IFNTUOG Schedule Telegram Bot
After=network.target

[Service]
User=root
Group=root

WorkingDirectory=/root/IFNTUOGrozkladTELEGRAMbot
ExecStart=/usr/bin/python3 /root/IFNTUOGrozkladTELEGRAMbot/bot_global.py

Restart=always
RestartSec=5
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
```

### 2. Активація та запуск

```bash
sudo systemctl daemon-reload
sudo systemctl enable nung_bot
sudo systemctl start nung_bot
```

### 3. Перевірка статусу та логів

```bash
sudo systemctl status nung_bot
```

Перегляд логів:

```bash
sudo journalctl -u nung_bot -f
```

Зупинка бота:

```bash
sudo systemctl stop nung_bot
```
