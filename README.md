# 🎓 IFNTUOG Schedule Bot (НУНГ Розклад)

Сучасний Telegram-бот для перегляду розкладу занять Івано-Франківського національного технічного університету нафти і газу (НУНГ).

Бот автоматично отримує дані з API деканату, генерує зручні зображення розкладу (замість тексту), надсилає автоматичні сповіщення та закріплює важливі повідомлення.

## 🔥 Можливості

* 🖼 **Графічний розклад:** Генерація стильних картинок з розкладом на день або тиждень.
* 📅 **Гнучка навігація:** Перегляд розкладу на сьогодні, завтра, поточний тиждень або будь-яку дату.
* 🔔 **Автоматичні сповіщення:**
  * **Щоденна розсилка** розкладу на сьогодні (о 5:00 ранку).
  * **Тижнева розсилка** у неділю (о 17:00) — можна вимкнути окремо.
  * Сповіщення автоматично **закріплюються** в чаті.
* 📌 **Розумне закріплення:** Бот автоматично управляє закріпленими повідомленнями (максимум 5), видаляючи старі при додаванні нових.
* 🔄 **Моніторинг змін:** Відстежує зміни в розкладі та повідомляє про них у реальному часі з автоматичним закріпленням.
* 🔎 **Пошук:**
  * `/search [текст]` — пошук предмета у розкладі вашої групи.
  * `/search_all [текст]` — глобальний пошук викладачів або аудиторій.
* 🛡 **Адміністрування:** Налаштування групи та керування сповіщеннями доступні лише адміністраторам чату.
* 🚫 **Розумна розсилка:** Бот не надсилає сповіщення, якщо на день/тиждень немає жодної пари.

## 🛠 Встановлення

### 1. Клонування репозиторію

```bash
git clone https://github.com/Dr34mChas3r/IFNTUOGrozkladTELEGRAMbot.git
cd IFNTUOGrozkladTELEGRAMbot
```

### 2. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 3. Налаштування середовища

Створіть файл `.env` у кореневій папці проєкту та додайте ваш токен:

```env
BOT_TOKEN=ваш_токен_від_BotFather
```

**Важливо:** Переконайтеся, що у системі встановлений шрифт (наприклад, `/usr/share/fonts/truetype/roboto/unhinted/RobotoTTF/Roboto-Regular.ttf`), який вказаний у налаштуваннях `bot_global.py`. Без нього текст на картинках може не відображатися.

### 4. Тестовий запуск

```bash
python3 bot_global.py
```

## 🤖 Команди бота

| Команда               | Опис                                        |
| --------------------- | ------------------------------------------- |
| `/start`              | Головне меню та привітання                  |
| `/group [Назва]`      | Встановити групу для чату (напр. `КІ-24-1`) |
| `/today`              | Отримати картинку розкладу на сьогодні      |
| `/tomorrow`           | Отримати картинку розкладу на завтра        |
| `/week`               | Отримати картинку розкладу на тиждень       |
| `/date [ДД.ММ]`       | Розклад на конкретну дату                   |
| `/search [текст]`     | Локальний пошук (у межах групи)             |
| `/search_all [текст]` | Глобальний пошук (викладач / аудиторія)     |
| `/settings`           | Налаштування сповіщень (тільки для адмінів) |

---

## ⚙️ Налаштування

### Доступні параметри в коді (`bot_global.py`):

```python
DAILY_NOTIFICATION_TIME = time(5, 0)      # Час щоденних сповіщень (5:00 ранку)
WEEKLY_NOTIFICATION_TIME = time(17, 0)    # Час тижневих сповіщень (17:00)
WEEKLY_NOTIFICATION_DAY = 6               # День тижня для тижневих сповіщень (6 = Неділя)
SCHEDULE_CHECK_INTERVAL = 30 * 60         # Інтервал перевірки змін (30 хв)
MAX_PINNED_MESSAGES = 5                   # Максимум закріплених повідомлень від бота
```

### Налаштування сповіщень через бота:

У меню `/settings` адміністратори можуть увімкнути/вимкнути:
- ✅ **Сповіщення про зміни** — миттєві повідомлення при зміні розкладу
- ✅ **Щоденно о 5:00** — розклад на сьогодні кожного ранку
- ✅ **Щотижнево** — розклад на наступний тиждень у неділю о 17:00

---

## 🤖 Налаштування автозапуску (systemd)

Щоб бот працював у фоновому режимі та автоматично перезапускався після перезавантаження сервера, створіть службу `systemd`.

### 1. Створення файлу служби

```bash
sudo nano /etc/systemd/system/nung_bot.service
```

Вставте наступний вміст (відредагуйте шляхи відповідно до вашого середовища):

```ini
[Unit]
Description=IFNTUOG Schedule Telegram Bot
After=network.target

[Service]
User=root
Group=root

WorkingDirectory=/root/IFNTUOGrozkladTELEGRAMbot
ExecStart=/usr/bin/python3 /root/IFNTUOGrozkladTELEGRAMbot/bot_global.py

Restart=always
RestartSec=5
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
```

### 2. Активація та запуск

```bash
sudo systemctl daemon-reload
sudo systemctl enable nung_bot
sudo systemctl start nung_bot
```

### 3. Перевірка статусу та логів

Статус служби:
```bash
sudo systemctl status nung_bot
```

Перегляд логів у реальному часі:
```bash
journalctl -u nung_bot -f
```

Останні 50 рядків логів:
```bash
journalctl -u nung_bot -n 50
```

Перезапуск бота:
```bash
sudo systemctl restart nung_bot
```

Зупинка бота:
```bash
sudo systemctl stop nung_bot
```

---

## 📋 Структура проєкту

```
IFNTUOGrozkladTELEGRAMbot/
├── bot_global.py              # Основний файл бота
├── image_gen.py               # Генератор зображень розкладу
├── .env                       # Конфігурація (BOT_TOKEN)
├── requirements.txt           # Python залежності
├── user_settings.json         # Налаштування користувачів (автоматично)
├── schedule_cache_global.json # Кеш розкладу (автоматично)
└── README.md                  # Документація
```

---

## 🐛 Діагностика проблем

### Бот не запускається

1. Перевірте логи:
   ```bash
   journalctl -u nung_bot -n 100
   ```

2. Переконайтесь, що BOT_TOKEN правильний у `.env`

3. Перевірте, чи встановлені всі залежності:
   ```bash
   pip install -r requirements.txt
   ```

### Картинки розкладу не генеруються

Перевірте наявність шрифту:
```bash
ls /usr/share/fonts/truetype/roboto/unhinted/RobotoTTF/Roboto-Regular.ttf
```

Якщо шрифту немає, встановіть:
```bash
sudo apt install fonts-roboto
```

### Бот не надсилає сповіщення

1. Перевірте, чи увімкнені сповіщення в `/settings`
2. Переконайтесь, що користувач є адміністратором чату
3. Перевірте часовий пояс у коді (має бути `Europe/Kyiv`)

---

## 📝 Ліцензія

MIT License

---

## 👨‍💻 Автор

Створено для студентів НУНГ | [GitHub](https://github.com/Dr34mChas3r/IFNTUOGrozkladTELEGRAMbot)

---

## 🤝 Внесок

Якщо ви знайшли баг або маєте ідею для покращення — створіть Issue або Pull Request!

---

## ⭐ Підтримка

Якщо бот вам допомагає — поставте зірку ⭐ на GitHub!